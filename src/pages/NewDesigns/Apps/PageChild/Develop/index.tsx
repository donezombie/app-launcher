import BaseUrl from 'consts/baseUrl';
import { Navigate } from 'react-router-dom';

const Develop = () => {
  //! State

  //! Function

  //! Render
  return <Navigate to={BaseUrl.Develop.UploadApps} />;
};

export default Develop;
